{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {},
  "deploy": {
    "startCommand": "gunicorn web_app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300 --log-level info",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
